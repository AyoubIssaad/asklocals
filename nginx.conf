server {
    listen 8005;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Handle 404s with the main page
    error_page 404 /index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
